# Updates

## 2025-11-30
- Created `backend/.env.example` and `backend/.env` to fix database connection issues during local development.
- Created `backend/.gitignore` to exclude environment files and binaries.
